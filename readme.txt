#This program uses face_recognition api of pyhton to detect faces from webcam input

#Fillow below steps to run face detection program
1. install opencv
	pip3 install opencv-python
2. install face recognition API
	pip install cmake
	pip install dlib
	pip install face_recognition
3. run program
	python face_detector.py
4. to exit the program press 'q' on keyboard

